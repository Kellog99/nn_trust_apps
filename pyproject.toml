[project]
name = "nn-trust-apps"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "nn_trust",
    "fastapi[standard]>=0.128.7",
    "uvicorn[standard]>=0.44.0",
    "reportlab>=4.4.10",
    "requests>=2.33.1",
    "ray>=2.55.0",
    "matplotlib>=3.10.8",
    "timm>=1.0.24",
]

[tool.uv.sources]
nn_trust = { path = "submodules/nn_trust", editable = true }